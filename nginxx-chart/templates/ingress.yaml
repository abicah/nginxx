apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginxx
spec:
  rules:
    - host: {{ .Values.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nginxx
                port:
                  number: {{ .Values.port }}
